from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Analyze viewer comments to gain insights into viewer feedback; whether a feedback is positive, neutral, or negative.',
    author='Isaac-Otom',
    license='',
)
